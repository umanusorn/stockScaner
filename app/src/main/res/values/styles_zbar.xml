<resources>
    <style name="ThemeZBar" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <!-- Base application theme. -->
    <style name="ThemeZBar.AppOverlayTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:actionBarStyle">@style/ThemeZBar.ActionBarStyle.Transparent</item>

        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>
    <style name="AppFullScreenTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!--@android:Widget.ActionBar-->

    <style name="ThemeZBar.ActionBarStyle.Transparent" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">#99000000</item>
        <item name="android:titleTextStyle">@style/ThemeZBar.ActionBarStyle.Transparent.TitleTextStyle</item>
        <item name="android:backgroundSplit">#99000000</item>
    </style>

    <!--@android:style/TextAppearance.Holo.Widget.ActionBar.Title-->
    <style name="ThemeZBar.ActionBarStyle.Transparent.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:windowTitleSize">@dimen/textsizeSubHeading</item>
    </style>

</resources>
